# examples 文档镜像

**源路径：** `examples/`

## 职责

可运行示例；README Quick Start **只能**引用本目录下已测试通过的示例。

## 目标示例

| 目录 | 说明 |
|------|------|
| `02_counter` | Web 计数器，验证 reactive + build |
| `04_multi_screen_game` | flow/free + 路由 |
| `05_escape_layer` | 逃生层与 signal 绑定 |

新增示例须配 `docs-src/examples/<name>.md` 说明意图与验证命令。
